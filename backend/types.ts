export type Player = {
  id: string
  x: number,
  y: number
}

export type gameData = {
  id: string
  gameId: string
}

export type positionData = {
  id: string
  x: number
  y: number
  gameId: string
}

export type leaveData = {
  id: string
}